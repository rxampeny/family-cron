name: Cron Emails

on:
  schedule:
    # 9:00 AM hora Madrid (8:00 UTC en invierno, 7:00 UTC en verano)
    - cron: '0 8 * * *'
    # 8:00 PM hora Madrid (19:00 UTC en invierno, 18:00 UTC en verano)
    - cron: '0 19 * * *'
  workflow_dispatch:

jobs:
  send-birthday-emails:
    if: github.event_name == 'workflow_dispatch' || github.event.schedule == '0 8 * * *'
    runs-on: ubuntu-latest
    steps:
      - name: Send birthday emails
        run: |
          curl -s -X POST \
            "${{ secrets.SUPABASE_URL }}/functions/v1/send-birthday-emails" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{}'

  send-reminders:
    if: github.event_name == 'workflow_dispatch' || github.event.schedule == '0 19 * * *'
    runs-on: ubuntu-latest
    steps:
      - name: Send reminders
        run: |
          curl -s -X POST \
            "${{ secrets.SUPABASE_URL }}/functions/v1/send-reminders" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{}'
